- name: Install Spotify
  community.general.flatpak:
    name: spotify
    state: present
  when: os.type == 'linux'

- name: Install Spotify
  community.general.homebrew_cask:
    name: spotify
    state: present
  when: os.type == 'osx'
