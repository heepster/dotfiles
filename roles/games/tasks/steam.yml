- name: Install Steam
  package:
    name: steam
    state: present
  when: os.type == 'linux'

- name: Install Steam
  community.general.homebrew_cask:
    name: steam
    state: present
  when: os.type == 'osx'

